const insertIntoDB = async (payload) => {};

const getAllFromDB = async (payload) => {};

const getByIdFromDB = async (id: string) => {};

const updateOneInDB = async (id: string, payload) => {};

const deleteByIdFromDB = async (id: string) => {};

export const offeredCourseService = {
  insertIntoDB,
  getAllFromDB,
  getByIdFromDB,
  updateOneInDB,
  deleteByIdFromDB,
};
